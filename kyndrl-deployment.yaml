apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyndrl-deployment
  namespace: shubha-ns
  labels:
    app: kyndrl-app
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: kyndrl
  template:
    metadata:
      labels:
        app: kyndrl
    spec:
      containers:
        - name: kyndrl-app-container
          image: muralisocial123/kyndrl-flower-app:v1.1.0
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"

---
apiVersion: v1
kind: Service
metadata:
  name: kyndrl-service
  namespace: shubha-ns
spec:
  selector:
    app: kyndrl
  ports:
    - port: 3003 #It is a service port no., we are using to expose aplication to the internet
      targetPort: 3015
      protocol: TCP
  type: ClusterIP
